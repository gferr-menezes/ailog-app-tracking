import 'package:flutter/material.dart';

class LatLongList extends StatelessWidget {
  const LatLongList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('LatLongList'),
    );
  }
}
